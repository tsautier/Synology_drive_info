v2.1.33
- Added support for HTTPS, including a checkbox in settings for manually added NAS.
- Added DSM version for each NAS.

v2.1.32
- Added checking that a Synology exists at the IP/port entered when manually adding a remote Synology NAS.
- Bug fix for not setting the permissions for creating/deleting scheduled tasks for the email option.
- Bug fix for USB drive's SMART info being logged with a blank serial number.

v2.1.31
- Added translations for the new settings options.
- Changed so package upgrades update the permissions instead of the updated package updating the permissions when it's frst run.
- Added Drive Info package version after "hostname IP-address model".
- Bug fix for not detecting HA-SMR or HM-SMR drives.

v2.1.30
- Added option to set a schedule to email changed SMART values (Not translated to other languages yet).
- Added drive size column.
- Added SMR column. Credit to @JoeSchmuck and @basilhendroff for their SMR drive list.
- Added showing the drives in each storage pool - hover the mouse over the RAID type to see them.
- Added showing which storage pool each drive is in - hover the mouse over the Drive's label to see it.
- Fixed short drive model from some USB drives/docks.

v2.0.29
- Added Volume information and status.
  - Defaults to enabled but can be disabled in the settings.
  - Shows Storage Pool status, including if data scrubbing is running.
- Can now show you the drive information for all local Synology NAS.
  - It can find all local Synology NAS itself.
  - Requires all local Synology NAS to have the same Drive Info version installed.
- Added a Settings menu where you can:
  - Enable or Disable showing drive info for all local Synology NAS.
  - Manually add local Synology NAS by hostname, IP and port (in case you only want to see drive information for certain NAS and not all local Synology NAS).
- Added a Reload button.
- Added drive temperature.
- Added SMART information. The text in the Status column for each drive is a link to the SMART info.
- Added an option in the settings to only show important SMART info.
- Added percentage to data scrubbing, RAID rebuilding/repairing status.
- Changed to show the hostname, IP address and model of the Synology that Drive Info is running on instead of "Drive Information".
- Changed to not show the Permissions ui page during upgrades.
- Added missing Thai package description.
- Added translations for all the error messages.
- Bug for Dutch showing "Show volume information" in English.

v2.0.28
- Added drive temperature.
- Added language translations to the SMART info for "Drive", "M.2 Drive", "System Drive" and "USB Drive". 
- Bug fix for "USB Drive" not correctly translated.
- Bug fix for USB drives SMART info.
- Bug fix for English showing the old package description in Package Center.

v2.0.27
- Updated package's description for all languages.
- Added missing Thai package description.
- Bug fix for SAS drives showing a lot of "debug: command not found" errors.

v2.0.26
- Bug fix for showing, or trying to show, SMART info for drives on the Synology running Drive Info when a drive from another local Synology was selected.

v2.0.25
- Added SMART information. The text in the Status column for each drive is a link to the SMART info.
- Added an option in the settings to only show important SMART info.
- Added percentage to data scrubbing, RAID rebuilding/repairing status.
- Added translations for all the error messages.
- Bug for Dutch showing "Show volume information" in English.
- Bug fix "Shows other local Synology NAS' info in the DSM language of the NAS running Drive Info".

v2.0.24
- Now shows Storage Pool status, including if data scrubbing is running.
- Bug fix for "Find all local Synology NAS"​ and "Show volume information"​ showing the wrong toggle state if you change a toggle and click cancel or back and then click settings again.

v2.0.23
- Now shows other local Synology NAS' info in the DSM language of the NAS running Drive Info.

v2.0.22
- Added Volume information and status.
  - Defaults to enabled but can be disabled in the settings.

v2.0.21
- Bug fix other local Synology NAS having 2 headings.

v2.0.20
- Changed to show the hostname, IP address and model of the Synology that Drive Info is running on instead of "Drive Information".
- Changed to not show the Permissions ui page during upgrades.
- Changed to show the remote Synology's hostnames in black instead of gray.
- Changed to show the IP address and model in black instead of gray, and a slightly larger font.
- Bug fix for showing English text when DSM's "Display Language" is set to "Browser default".
- Bug fix for DSM 7.1.1 showing the drives' health as "Unknown (null)".
- Bug fix for showing drive health "Unknown (data_detected)" instead of "Detected".
- Bug fix for showing drive health "Unknown (disknotsupported)" instead of "Not supported".
- Bug fix for some models showing pv10-j after the + in the model name.
- Bug fix for some models showing -j instead of the + in the model name.

v2.0.19
- Can now show you the drive information for all local Synology NAS.
  - It can find all local Synology NAS itself.
  - Requires all local Synology NAS to have Drive Info v2 or later installed.
- Added a Settings menu where you can:
  - Enable or Disable showing drive info for all local Synology NAS.
  - Manually add local Synology NAS by hostname, IP and port (in case you only want to see drive information for certain NAS and not all local Synology NAS).

v1.0.18
- Improved sorting of drives when NAS has more than 9 drives.
- Updated to show PCIe M.2 card model if M.2 SATA drive is in M2D18 or M2D17.
- Changed to list M.2 SATA drives after HDDs/2.5" SSDs and NVMes.

v1.0.17
- Bug fix for v1.0.16 DSM 6 package having minimum DSM version 7.0.

v1.0.16
- Bug fix for DSM 6 showing the wrong description when hovering your mouse over the desktop icon.

v1.0.15
- Added translations of "Loading..." text for non English speakers.
- Added support for DSM 6.
- Now hides Location column if: 
  - No drives are in an expansion unit.
  - No M.2 drives are in a E10M20-T1, M2D20, M2D18 or M2D17 PCIe card.
  - It's not a HD6500 with a dedicated system drive.
- Bug fix for getting SAS drives' serial number via smartctl if `cat "/sys/block/$drive/device/syno_disk_serial"` failed.

v1.0.14
- Added health status column.
- Added "System Drive" to location column for HD6500 system drive.
- Added a spinner and "Loading drive information…" text.
  - Getting each drive's health status takes around 0.5 seconds per drive.
- Changed font family, size and colors to better match DSM.
- Made the window's default size wider.

v1.0.13
- Moved expansion unit and M.2 PCIe cards to their own "Location" column.
- Improved language translations.
- Improved column spacing.
- Bug fix for drive model and serial in wrong columns for drives with no location.
  - i.e. Drives not in an expansion unit or M.2 PCIe card.

v1.0.12
- Moved expansion unit and M.2 PCIe cards to their own column.
- Improved language translations.

v1.0.11
- Added language support for the install/upgrade ui permissions steps pages.
- Added language support for error messages.

v1.0.10
- Added support for the languages supported by DSM.
- Bug fix for description being shown as the name.

v1.0.9
- Added support for the languages supported by DSM.

v1.0.8
- Added PCIe M.2 card model to M.2 drives that are in a PCIe card.
- Adjusted column widths and added nowrap.

v1.0.7
- Changed font in table content.
- Fixed steps to setting the permission.

v1.0.6
- First release.
